{
  "name": "quorum",
  "version": "0.0.1",
  "private": true,
  "description": "Pay for verified compute on Kaspa: a result is paid only when independent workers agree, and a worker that fabricates is slashed.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --import tsx --test src/**/*.test.ts",
    "typecheck": "tsc --noEmit",
    "harvest": "tsx tools/harvest.ts"
  },
  "dependencies": {
    "@noble/hashes": "^1.8.0",
    "metered-protocol": "^2.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.20.2",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
